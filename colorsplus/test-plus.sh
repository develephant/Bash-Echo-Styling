#!/bin/bash

#source colorsplus
. colorsplus

echo -e "${CRED}This is red text${CEND}"

echo -e "${FBLD}This is bold default text.${FEND}"

echo -e "This is ${FUL}${CCYN}underlined.${FIN}"

echo -e "This is ${CBLU}Blue and ${FBLD}now bold.${FIN}"

echo -e "Test short ${RED}Red${FIN}"